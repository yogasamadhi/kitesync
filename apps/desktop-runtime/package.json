{
  "name": "@kitesync/desktop-runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "bun run --filter @kitesync/contracts build",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json --noEmit && esbuild src/main.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/main.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kitesync/contracts": "workspace:*",
    "@sinclair/typebox": "^0.34.41",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
